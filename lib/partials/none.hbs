      {{#if @first}}
      function () {
      {{else}}
    , function () {
      {{/if}}
        {{#if isAcceptedState}}
        lastAcceptIndex = i
        lastAcceptState = {{@index}}
        {{/if}}


{{>fallbackPartial}}
      }
